# remove me (app.yaml.ctmpl) once we can update fiabs to not expect a template to render

runtime: python37
entrypoint: gunicorn -b :$PORT main:app
basic_scaling:
  max_instances: 100

handlers:
- url: /.*
  script: auto
